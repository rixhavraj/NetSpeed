server:
  port: ${PORT:8080}

spring:
  application:
    name: backend

  servlet:
    multipart:
      max-file-size: 50MB
      max-request-size: 50MB

app:
  version: ${APP_VERSION:1.0.5}
  download-url: ${APP_DOWNLOAD_URL:/api/downloads/net-speed-v1.0.5.exe}
  file-name: ${APP_FILE_NAME:net-speed-v1.0.5.exe}
  file-size: ${APP_FILE_SIZE:2.2MB}
  changelog: ${APP_CHANGELOG:Upgraded to precision physical-only interface monitoring with GetIfTable2 and std::chrono clock delta timing}
  download-dir: ${APP_DOWNLOAD_DIR:}
  cors:
    allowed-origins: ${APP_CORS_ALLOWED_ORIGINS:http://localhost:5173,https://rixhavraj.github.io}
